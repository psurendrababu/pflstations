@model IEnumerable<PipelineFeatureList.Models.OutsideDiameter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OutsideDiameterItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OutsideDiameterItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OutsideDiameterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OutsideDiameterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OutsideDiameterID })
        </td>
    </tr>
}

</table>
