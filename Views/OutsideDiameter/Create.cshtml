@model PipelineFeatureList.Models.OutsideDiameter

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>OutsideDiameter</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.OutsideDiameterItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OutsideDiameterItem)
            @Html.ValidationMessageFor(model => model.OutsideDiameterItem)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
