@model PipelineFeatureList.Models.UploadDirectory

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>UploadDirectory</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Directory)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Directory)
            @Html.ValidationMessageFor(model => model.Directory)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DatabaseName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DatabaseName)
            @Html.ValidationMessageFor(model => model.DatabaseName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
