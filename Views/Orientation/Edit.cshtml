@model PipelineFeatureList.Models.Orientation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Orientation</legend>

        @Html.HiddenFor(model => model.OrientationID)

        <div class="editor-label">
            @Html.LabelFor(model => model.OrientationItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OrientationItem)
            @Html.ValidationMessageFor(model => model.OrientationItem)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
