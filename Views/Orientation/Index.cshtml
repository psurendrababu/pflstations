@model IEnumerable<PipelineFeatureList.Models.Orientation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrientationItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrientationItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrientationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrientationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrientationID })
        </td>
    </tr>
}

</table>
