@model PipelineFeatureList.Models.RecordIdentifier

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>RecordIdentifier</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.RecordIdentifierItem)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RecordIdentifierItem)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
