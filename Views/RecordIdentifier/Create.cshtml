@model PipelineFeatureList.Models.RecordIdentifier

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>RecordIdentifier</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.RecordIdentifierItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.RecordIdentifierItem)
            @Html.ValidationMessageFor(model => model.RecordIdentifierItem)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
