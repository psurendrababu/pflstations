@model IEnumerable<PipelineFeatureList.Models.RecordIdentifier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RecordIdentifierItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RecordIdentifierItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecordIdentifierID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecordIdentifierID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecordIdentifierID })
        </td>
    </tr>
}

</table>
