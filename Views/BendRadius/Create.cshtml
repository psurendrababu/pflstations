@model PipelineFeatureList.Models.BendRadius

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>BendRadius</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.BendRadiusItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BendRadiusItem)
            @Html.ValidationMessageFor(model => model.BendRadiusItem)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
