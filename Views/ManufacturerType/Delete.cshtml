@model PipelineFeatureList.Models.ManufacturerType

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ManufacturerType</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.ManufacturerTypeItem)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ManufacturerTypeItem)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
