@model PipelineFeatureList.Models.ManufacturerType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ManufacturerType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ManufacturerTypeItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ManufacturerTypeItem)
            @Html.ValidationMessageFor(model => model.ManufacturerTypeItem)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
