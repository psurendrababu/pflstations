@model PipelineFeatureList.Models.SeamType

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>SeamType</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.SeamTypeItem)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.SeamTypeItem)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.SeamTypeID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
