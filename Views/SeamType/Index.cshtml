@model IEnumerable<PipelineFeatureList.Models.SeamType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SeamTypeItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SeamTypeItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SeamTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SeamTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SeamTypeID })
        </td>
    </tr>
}

</table>
