@model PipelineFeatureList.Models.SeamType

@{
    ViewBag.Title = "Delete Seam Weld Type";
}

<h2>Delete Seam Weld Type</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>SeamType</legend>
    <table>
        <tr>
            <td>
                <div class="display-label">
                    @Html.DisplayNameFor(model => model.SeamTypeItem)
                </div>
            </td>
            <td>
                <div class="display-field">
                    @Html.DisplayFor(model => model.SeamTypeItem)
                    @Html.ValidationMessageFor(model => model.SeamTypeItem)

                </div>
            </td>
        </tr>
        <tr>
            <td>
                <div class="display-label">
                    @Html.DisplayNameFor(model => model.JointFactor)
                </div>
            </td>
            <td>
                <div class="display-field">
                    @Html.DisplayFor(model => model.JointFactor)
                </div>
            </td>
        </tr>
    </table>
</fieldset>
@using (Html.BeginForm()) {
<p>
    @if (ViewBag.HasError != "True")
    {
        <input type="submit" value="Delete" />
    }
    @Html.ActionLink("Back to List", "Index", null, htmlAttributes: new { @class = "linkbutton" })
</p>
}
