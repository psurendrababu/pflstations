@model IEnumerable<PipelineFeatureList.Models.StandardEntry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StandardEntryItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StandardEntryItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StandardEntryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StandardEntryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StandardEntryID })
        </td>
    </tr>
}

</table>
