@model PipelineFeatureList.Models.StandardEntry

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>StandardEntry</legend>

        @Html.HiddenFor(model => model.StandardEntryID)

        <div class="editor-label">
            @Html.LabelFor(model => model.StandardEntryItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StandardEntryItem)
            @Html.ValidationMessageFor(model => model.StandardEntryItem)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
