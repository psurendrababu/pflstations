@model IEnumerable<PipelineFeatureList.Controllers.GroupClassification>

@{
    ViewBag.Title = "Group Classifications";
}

<h2>User Types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GroupClassificationItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GroupClassificationItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GroupClassificationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GroupClassificationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GroupClassificationID })
        </td>
    </tr>
}
<div>
    @Html.ActionLink("Back to Admin", "Index", "Admin")
</div>
</table>
