@model PipelineFeatureList.Models.DisplayGroup

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DisplayGroup</legend>

        @Html.HiddenFor(model => model.DisplayGroupID)

        <div class="editor-label">
            @Html.LabelFor(model => model.DisplayGroupName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DisplayGroupName)
            @Html.ValidationMessageFor(model => model.DisplayGroupName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
