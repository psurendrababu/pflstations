@model IEnumerable<PipelineFeatureList.Models.DisplayGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DisplayGroupName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DisplayGroupName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DisplayGroupID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DisplayGroupID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DisplayGroupID })
        </td>
    </tr>
}

</table>
