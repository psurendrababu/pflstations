@model PipelineFeatureList.Models.DisplayGroup

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>DisplayGroup</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.DisplayGroupName)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DisplayGroupName)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.DisplayGroupID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
