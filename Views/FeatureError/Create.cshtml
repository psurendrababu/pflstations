@model PipelineFeatureList.Models.FeatureError

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FeatureError</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.FeatureErrorItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FeatureErrorItem)
            @Html.ValidationMessageFor(model => model.FeatureErrorItem)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ErrorText)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ErrorText)
            @Html.ValidationMessageFor(model => model.ErrorText)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.OrderBy)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OrderBy)
            @Html.ValidationMessageFor(model => model.OrderBy)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
