@model PipelineFeatureList.Models.ConstructionType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ConstructionType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ConstructionTypeItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ConstructionTypeItem)
            @Html.ValidationMessageFor(model => model.ConstructionTypeItem)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
