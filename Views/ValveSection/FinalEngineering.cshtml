@model IEnumerable<PipelineFeatureList.Models.ValveSectionFinalEngineering>

@{
    ViewBag.Title = "FinalEngineering";
}

@section Scripts {
  @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript">
        function onchangeevent1(e) {
            var selectedItem = $("#ddlPipeSystems").val();
            var selectedItem2 = $("#ddlPipelines").val();
            if (selectedItem == "") selectedItem = 0;
            if (selectedItem2 == "") selectedItem2 = 0;
            $.ajax({
                cache: false,
                type: "GET",
                url: '/ValveSection/FinalEngineeringDropDowns/',
                data: { "id1": selectedItem, "id2": selectedItem2 },
                dataType: 'json',
                success: function (data) {
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    //alert('Failed to ...');
                }
            });
        }
    </script>
    <script type="text/javascript">
        function onchangeevent2(e) {
            var selectedItem = $("#ddlPipeSystems").val();
            var selectedItem2 = $("#ddlPipelines").val();
            if (selectedItem == "") selectedItem = 0;
            if (selectedItem2 == "") selectedItem2 = 0;
            $.ajax({
                cache: false,
                type: "GET",
                url: '/ValveSection/FinalEngineeringDropDowns/',
                data: { "id1": selectedItem, "id2": selectedItem2 },
                dataType: 'json',
                success: function (data) {
                },
                error: function (xhr, ajaxOptions, thrownError) {
                    //alert('Failed to ...');
                }
            });
        }
    </script>
    <script type="text/javascript">
        $(document).ready(function () {
            onchangeevent1(null);
            onchangeevent2(null);
        });
    </script>
}

<h2>Final Engineering</h2>

<h3>Assigned to @Html.Raw(Session["UserName"].ToString())</h3>

    @Html.Partial("FinalEngineeringAssigned")

<hr />

<h3>Pool</h3>
    @Html.Partial("FinalEngineeringPool")