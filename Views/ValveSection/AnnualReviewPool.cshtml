@model IEnumerable<PipelineFeatureList.Models.ValveSectionAnnualReview>

@{
    ViewBag.Title = "AnnualReview";
}

@using (Html.BeginForm("AnnualReview", "ValveSection", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AnnualReview</legend>
        
        <table width="100%">
            <tr>
            <td>&nbsp;&nbsp;&nbsp;&nbsp;</td>
            <td><div class="editor-label">@Html.Raw("Station System")</div></td>
            <td><div class="editor-field">@Html.DropDownList("PipeSystemData", ViewBag.PipeSystemID as SelectList, "- All Station Location -", new { @id = "ddlPipeSystems", @onchange = "onchangeevent1()" })</div></td>
            <td><div class="editor-label">@Html.Raw("Station")</div></td>
            <td><div class="editor-field">@Html.DropDownList("PipelineData", ViewBag.PipelineID as SelectList, "- All Stations -", new { @id = "ddlPipelines", @onchange = "onchangeevent2()" })</div></td>
            <td><input type="submit" value="Filter" onclick="onchangeevent2()" /></td>
            </tr>
        </table>
    </fieldset>
}

<table width="100%">
    <tr>
        <th>
            @Html.Raw("Station Location")
        </th>
        <th>
            @Html.Raw("Station")
        </th>
        <th>
            @Html.Raw("Circuit")
        </th>
        <th>
            @Html.Raw("Status")
        </th>
    </tr>

@foreach (var item in Model) {
    if (item.ValveSectionAnnualReviewPool != null)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValveSectionAnnualReviewPool.PipeSystem.PipeSystemItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValveSectionAnnualReviewPool.Pipeline.PipelineItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValveSectionAnnualReviewPool.ValveSection.ValveSectionItem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValveSectionAnnualReviewPool.ValveStatus.ValveSectionStatusItem)
            </td>
            <td>
                @Html.ActionLink("Overview", "", "Overview", new { ValveSectionID = item.ValveSectionAnnualReviewPool.ValveSection.ValveSectionID }, htmlAttributes: new { @class = "linkbutton" })
            </td>
        </tr>
    }
}

</table>

