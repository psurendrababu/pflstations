@model PipelineFeatureList.Controllers.UserType

@{
    ViewBag.Title = "Create User Type";
}

<h2>Create User Type</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>UserType</legend>
        <table><tr><td>
        <div class="editor-label">
            @Html.LabelFor(model => model.UserTypeItem)
        </div></td>
        <td>
        <div class="editor-field">
            @Html.EditorFor(model => model.UserTypeItem)
            @Html.ValidationMessageFor(model => model.UserTypeItem)
        </div>
        </td></tr></table>
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
