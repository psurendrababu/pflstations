@model PipelineFeatureList.Models.HCA

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>HCA</legend>

        @Html.HiddenFor(model => model.HCAID)

        <div class="editor-label">
            @Html.LabelFor(model => model.HCAItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.HCAItem)
            @Html.ValidationMessageFor(model => model.HCAItem)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
