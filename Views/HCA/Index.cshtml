@model IEnumerable<PipelineFeatureList.Models.HCA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HCAItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HCAItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HCAID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HCAID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HCAID })
        </td>
    </tr>
}

</table>
