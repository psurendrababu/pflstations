@model PipelineFeatureList.Models.ReportingGroup

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ReportingGroup</legend>

        @Html.HiddenFor(model => model.ReportingGroupID)

        <div class="editor-label">
            @Html.LabelFor(model => model.ReportingGroupName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ReportingGroupName)
            @Html.ValidationMessageFor(model => model.ReportingGroupName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
