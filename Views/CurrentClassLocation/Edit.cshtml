@model PipelineFeatureList.Models.CurrentClassLocation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CurrentClassLocation</legend>

        @Html.HiddenFor(model => model.CurrentClassLocationID)

        <div class="editor-label">
            @Html.LabelFor(model => model.CurrentClassLocationItem)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CurrentClassLocationItem)
            @Html.ValidationMessageFor(model => model.CurrentClassLocationItem)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
