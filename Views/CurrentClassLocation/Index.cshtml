@model IEnumerable<PipelineFeatureList.Models.CurrentClassLocation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentClassLocationItem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentClassLocationItem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrentClassLocationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrentClassLocationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrentClassLocationID })
        </td>
    </tr>
}

</table>
