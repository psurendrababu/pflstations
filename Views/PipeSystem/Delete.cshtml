@model PipelineFeatureList.Models.PipeSystem

@{
    ViewBag.Title = "Delete Station Location";
}

<h2>Delete Station Location</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>PipeSystem</legend>

    <table><tr><td>
    <div class="display-label">
         @Html.DisplayNameFor(model => model.PipeSystemItem)
    </div></td>
    <td>
        <div class="display-field">
            @Html.DisplayFor(model => model.PipeSystemItem)
            @Html.ValidationMessageFor(model => model.PipeSystemItem)
        </div>
    </td></tr></table>
</fieldset>
@using (Html.BeginForm()) {
<p>
    @if (ViewBag.HasError != "True")
    {
        <input type="submit" value="Delete" />
    }
    @Html.ActionLink("Back to List", "Index", null, htmlAttributes: new { @class = "linkbutton" })
</p>
}
