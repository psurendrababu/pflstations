@model PipelineFeatureList.Models.PipeSystem

@{
    ViewBag.Title = "Edit Station Location";
}

<h2>Edit Station Location</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PipeSystem</legend>

        @Html.HiddenFor(model => model.PipeSystemID)

        <table><tr><td>
        <div class="editor-label">
            @Html.LabelFor(model => model.PipeSystemItem)
        </div></td>
        <td>
        <div class="wide">
            @Html.EditorFor(model => model.PipeSystemItem)
            @Html.ValidationMessageFor(model => model.PipeSystemItem)
        </div>
        </td></tr></table>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

    <div>
        @Html.ActionLink("Back to List", "Index", null, htmlAttributes: new { @class = "linkbutton" })
    </div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
